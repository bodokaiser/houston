// Package httpd provides components to build a HTTP service.
//
// We are using the echo HTTP framework for routing and middleware. At the top
// level we provide extensions to the echo framework. HTTP route handlers are
// defined in the subpackage.
package httpd
